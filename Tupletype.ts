// when adding ? to type, it waits for the upcoming value
// when arrays type are declared in special way it is known as tuple
let role: [string, string, number, number, boolean?] = [
  "admin",
  "manager",
  1,
  3,
];
role.push(true);
