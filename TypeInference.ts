// If we want a specific value to be assigned we can do it as follows
let data: 5 = 5; // this only allows to assign it specific value 5
const val = 10; // const value can never be changed so, the type is be default 10
